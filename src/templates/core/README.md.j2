# {{ project_name | default("My Project") }}

> {{ description | default("A new project generated by PAPER-CODE") }}

## 🚀 Overview

Welcome to **{{ project_name | default("My Project") }}**. This project is built using **{{ tech_stack | default("Modern Tech Stack") }}**{% if project_type | default("") %} and follows standard **{{ project_type }}** architecture{% endif %}.

This repository contains comprehensive documentation to help developers and AI assistants understand the codebase, architecture, and coding standards.

## 📚 Documentation

We maintain detailed documentation in the `docs/` directory:

- **[Architecture Guide](docs/ARCHITECTURE.md)**: High-level overview of the system design.
- **[Code Standards](docs/CODE_STANDARDS.md)**: Guidelines for variables, naming conventions, and formatting.
- **[Contributing](docs/CONTRIBUTING.md)**: How to set up the environment and submit PRs.
- **[Testing](docs/TESTING.md)**: Strategy and commands for running tests.
- **[Deployment](docs/DEPLOYMENT.md)**: CI/CD pipelines and deployment instructions.
- **[AI Rules](docs/ai/AI_RULES.md)**: **Important** context for AI assistants (Cursor, Copilot).

## 🛠️ Tech Stack

This project relies on the following core technologies:

- **Stack**: {{ tech_stack | default("Modern Tech Stack") }}
{% if libraries | default([]) %}
- **Key Libraries/Modules**:
{% for lib in libraries %}
  - {{ lib }}
{% endfor %}
{% endif %}

## 🏁 Getting Started

### Prerequisites
{% if tech_stack | default("") and ("Node" in tech_stack or "React" in tech_stack or "Vue" in tech_stack or "Next" in tech_stack or "Svelte" in tech_stack) %}
- Node.js (LTS version recommended)
- npm or yarn or pnpm
{% elif tech_stack | default("") and ("Python" in tech_stack or "FastAPI" in tech_stack or "Django" in tech_stack) %}
- Python 3.11+
- pip or poetry
{% elif tech_stack | default("") and "Go" in tech_stack %}
- Go 1.22+
{% elif tech_stack | default("") and "Rust" in tech_stack %}
- Cargo & Rustc
{% else %}
- Check the official documentation for {{ tech_stack | default("the selected technology") }}.
{% endif %}

### Installation

1. Clone the repository:
   ```bash
   git clone https://github.com/your-org/{{ project_name | lower | replace(" ", "-") }}.git
   cd {{ project_name | lower | replace(" ", "-") }}
   ```

2. Install dependencies:
{% if tech_stack | default("") and ("Node" in tech_stack or "React" in tech_stack or "Vue" in tech_stack or "Next" in tech_stack) %}
   ```bash
   npm install
   # or
   yarn install
   ```
{% elif tech_stack | default("") and ("Python" in tech_stack or "FastAPI" in tech_stack or "Django" in tech_stack) %}
   ```bash
   pip install -r requirements.txt
   ```
{% elif tech_stack | default("") and "Go" in tech_stack %}
   ```bash
   go mod download
   ```
{% elif tech_stack | default("") and "Rust" in tech_stack %}
   ```bash
   cargo build
   ```
{% else %}
   ```bash
   # Add installation command here
   ```
{% endif %}

3. Run the development server:
{% if tech_stack | default("") and ("Node" in tech_stack or "React" in tech_stack or "Vue" in tech_stack or "Next" in tech_stack) %}
   ```bash
   npm run dev
   ```
{% elif tech_stack | default("") and "FastAPI" in tech_stack %}
   ```bash
   uvicorn main:app --reload
   ```
{% elif tech_stack | default("") and "Django" in tech_stack %}
   ```bash
   python manage.py runserver
   ```
{% elif tech_stack | default("") and "Go" in tech_stack %}
   ```bash
   go run main.go
   ```
{% elif tech_stack | default("") and "Rust" in tech_stack %}
   ```bash
   cargo run
   ```
{% else %}
   ```bash
   # Add run command here
   ```
{% endif %}

## ⚖️ License

Distributed under the MIT License. See [LICENSE](LICENSE) for more information.