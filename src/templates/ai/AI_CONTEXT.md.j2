# 🗺️ AI Project Context

## Project Architecture
This project follows a **{{ project_type | default("standard") }}** architecture pattern.

### Directory Map
- `src/` (or `app/`): Main source code.
- `docs/`: Documentation.
- `docs/ai/`: Rules for you (the AI).
- `docs/libs/`: Specific documentation for third-party libraries used here.

## 📚 Library Reference
The following libraries are integrated. Refer to their specific documentation snippets before generating code:

{% if libraries | default([]) %}
{% for lib in libraries %}
- **{{ lib }}**: See `docs/libs/{{ lib | lower | replace(" ", "") | replace(".", "") }}.md`
{% endfor %}
{% else %}
- *No specific external library docs generated.*
{% endif %}

## 🔄 Data Flow
{% if tech_stack | default("") and ("React" in tech_stack or "Next" in tech_stack) %}
- **UI** triggers actions -> **Hooks** manage side effects -> **API Layer** calls Backend.
{% elif libraries | default([]) | select("in", ["Redux", "Redux Toolkit"]) | list | length > 0 %}
- **Action** dispatched -> **Reducer** updates Store -> **Selector** updates UI.
{% elif tech_stack | default("") and "FastAPI" in tech_stack %}
- **Router** receives request -> **Controller/Service** processes logic -> **Repository** accesses DB -> **Schema** formats response.
{% else %}
- Standard MVC/MVVM flow applies.
{% endif %}

## 🎨 Design System
{% if libraries | default([]) | select("in", ["TailwindCSS"]) | list | length > 0 %}
- We use **Tailwind CSS**. 
- Primary Color: Refer to `tailwind.config.js`.
- Spacing: Use standard Tailwind spacing (p-4, m-2, etc.).
{% elif libraries | default([]) | select("in", ["Material", "MUI", "Angular Material"]) | list | length > 0 %}
- We use **Material UI**. Use the `sx` prop or styled components.
{% else %}
- Follow existing CSS patterns in the codebase.
{% endif %}