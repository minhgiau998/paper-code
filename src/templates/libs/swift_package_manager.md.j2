# Swift Package Manager

Official package manager for Swift projects.

Install: Integrated into Xcode/Swift toolchain.

Quick example:
```swift
// swift-tools-version:5.6
Package(
  name: "MyLib",
  dependencies: [ .package(url: "https://github.com/owner/repo.git", from: "1.0.0") ]
)
```