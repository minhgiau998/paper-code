# SwiftUI

Apple's modern UI toolkit for building declarative interfaces on iOS.

Install: Built into iOS SDK (use Xcode).

Quick example:
```swift
struct ContentView: View { var body: some View { Text("Hello, SwiftUI") } }
```